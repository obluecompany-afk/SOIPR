{% comment %}
  Cart drawer skeleton — Haravan có cart object built-in (cart.item_count, cart.items, cart.total_price).
  Sẽ port chi tiết từ components/CartDrawer.tsx ở Step 2.2.
  Lưu ý: ở Haravan, cart logic do platform handle — không cần localStorage hay React Context.
{% endcomment %}
